# Character models (map + studio)

Put these files in this folder:

- `character_male.glb` (preferred) or `character_male.gltf`
- `character_female.glb` (preferred) or `character_female.gltf`

The map loads `/characters/character_{gender}.glb` first, then falls back to `.gltf`.
